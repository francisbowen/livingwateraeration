{% if section.settings.sidebar_banner_enable %}
    <div class="velaBannerSidebar">
        <div class="velaContent">
            <div class="velaBanner">
                <div class="{{ section.settings.effect }}">
                    {% if section.settings.link != blank %}<a href="{{ section.settings.link }}" title="">{% endif %}
                        {% if section.settings.image != blank %}
                            <img class="img-responsive" alt="" src="{{ section.settings.image | img_url: "master" }}" />
                        {% else %}
                            <img class="img-responsive" alt="{{ shop.name }}" src="//placehold.it/300x370.jpg" />
                        {% endif %}
                    {% if section.settings.link != blank %}</a>{% endif %}
                </div>
            </div>
        </div>
    </div>
{% endif %}
{% schema %}
    {
        "name": "Sidebar Banner",
        "settings": [
            {
                "type": "header",
                "content": "Sidebar Banner"
            },
            {
                "type": "checkbox",
                "id": "sidebar_banner_enable",
                "label": "Enable Banner Sidebar?"
            },
            {
                "type": "text",
                "id": "link",
                "label": "Image Link"
            },
            {
                "type": "image_picker",
                "id": "image",
                "label": "Banner Image"
            },
            {
                "type": "select",
                "id": "effect",
                "label": "Effect",
                "default": "effectOne",
                "options": [
                    {
                        "value": "effectOne",
                        "label": "Effect one"
                    },
                    {
                        "value": "effectTwo",
                        "label": "Effect two"
                    }
                ]
            }
        ]
    }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}